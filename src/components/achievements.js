import classes from "../styles/Achievements.module.scss";

const Achievements = () => {
    return (
        <div className={classes.rootAchievements}>
            <div className={classes.heading}>
                <p>Achievements</p>
            </div>
        </div>
    );
};
